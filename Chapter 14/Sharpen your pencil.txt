Each of these widgets (user interface objects) is the
source of one or more events. Match the widgets with
the events they might cause. Some widgets might be a
source of more than one event, and some events can be
generated by more than one widget.
Page(470)

Widgets                     Event methods


check box            -       itemStateChanged(),mousePressed()

text field           -       focusGained(),mousePressed()

scrolling list       -      itemStateChanged(),mousePressed(),focusGained()

button               -      actionPerformed(),mousePressed(), mouseExited(),

dialog box           -      windowClosing()

radio button         -      actionPerformed(),itemStateChanged()

menu item            -      actionPerformed()